// Tells the compiler that we're using the standard library
#include <stdio.h>

// All programs start at main()
int main() {
  // printf doesn't apply new lines automatically
  printf("hello, world \n");
}
